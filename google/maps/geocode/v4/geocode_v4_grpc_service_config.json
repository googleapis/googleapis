{
  "methodConfig": [{
    "name": [
      { "service": "google.maps.geocode.v4.GeocodeService", "method": "GeocodeAddress" },
      { "service": "google.maps.geocode.v4.GeocodeService", "method": "GeocodeLocation" },
      { "service": "google.maps.geocode.v4.GeocodeService", "method": "GeocodePlace" },
      { "service": "google.maps.geocode.v4.DestinationService", "method": "SearchDestinations"}
    ],
    "timeout": "60s",
    "retryPolicy": {
      "maxAttempts": 5,
      "initialBackoff": "1s",
      "maxBackoff": "10s",
      "backoffMultiplier": 1.3,
      "retryableStatusCodes": ["UNAVAILABLE"]
    }
  }]
}