{
  "methodConfig": [{
    "name": [{
      "service": "google.cloud.workloadidentity.v1.WorkloadIdentity",
      "method": "GenerateServiceAgents"
    }],
    "timeout": "60s",
    "retryPolicy": {
      "maxAttempts": 5,
      "initialBackoff": "1s",
      "maxBackoff": "10s",
      "backoffMultiplier": 1.3,
      "retryableStatusCodes": ["UNAVAILABLE"]
    }
  }]
}