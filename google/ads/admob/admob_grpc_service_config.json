{
  "methodConfig": [{
    "name": [
      { "service": "google.ads.admob.v1.AdMobApi" }
    ],
    "timeout": "120s",
    "retryPolicy": {
      "maxAttempts": 5,
      "initialBackoff": "1s",
      "maxBackoff": "10s",
      "backoffMultiplier": 1.3,
      "retryableStatusCodes": ["UNAVAILABLE"]
    }
  }]
}
