{
  "methodConfig": [{
    "name": [
      { "service": "google.shopping.merchant.inventories.v1.LocalInventoryService" },
      { "service": "google.shopping.merchant.inventories.v1.RegionalInventoryService" }
    ],
    "timeout": "60s",
    "retryPolicy": {
      "maxAttempts": 5,
      "initialBackoff": "1s",
      "maxBackoff": "10s",
      "backoffMultiplier": 1.3,
      "retryableStatusCodes": ["UNAVAILABLE"]
    }
  }]
}
