{
  "methodConfig": [{
    "name": [
      { "service": "google.shopping.merchant.products.v1.ProductInputsService" },
      { "service": "google.shopping.merchant.products.v1.ProductsService" }
    ],
    "timeout": "60s",
    "retryPolicy": {
      "maxAttempts": 5,
      "initialBackoff": "1s",
      "maxBackoff": "10s",
      "backoffMultiplier": 1.3,
      "retryableStatusCodes": ["UNAVAILABLE"]
    }
  }]
}
